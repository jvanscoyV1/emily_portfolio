import Typography from "typography";
import moragaTheme from 'typography-theme-moraga';


// moragaTheme.headerFontFamily = ['Georgia']

const typography = new Typography(moragaTheme);

export default typography;

